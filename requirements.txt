# Core runtime (CPU fallback mode)
torch>=2.4.0
numpy>=1.24.0
matplotlib>=3.7.0
pandas>=2.0.0

# GPU / Triton compiler (install on CUDA-capable machines only)
# triton>=3.0.0

# Development & testing
pytest>=8.0.0
pytest-cov>=5.0.0
